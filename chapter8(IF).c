#include <stdio.h>

int main (void){

    /*for(int num = 1; num < 100; num++) { 08-01-01 문제
        if (num % 7 == 0 && num % 9 ==0){
            printf("%d 는 7과 9의 배수입니다. \n" , num);
        }

        else if (num % 7 == 0) {
            printf("%d 는 7의 배수입니다. \n" , num);
        }

        else if (num % 9 == 0) {
            printf("%d 는 9의 배수입니다. \n" , num);
        }


    }

    int num1, num2, total; 08-01-02 문제

    printf("숫자를 입력하세요 : ");
    scanf("%d", &num1);

    printf("숫자를 입력하세요 : ");
    scanf("%d", &num2);

    if(num1 >= num2){

        total = num1 - num2;

        printf("%d - %d = %d ", num1,num2,total);
    }

    else {
        total = num2- num1;

        printf("%d - %d = %d ", num2,num1,total);
    }

    int Score;  08-01-03 문제

    printf("점수를 입력하시오");
    scanf("%d" , &Score);

    if (Score >= 90){
        printf("학점이 A 입니다.");
    }

    else if (Score < 90 && Score >=80)
    {
        printf("학점이 B 입니다.")
    }

    else if (Score < 80 && Score >=70)
    {
        printf("학점이 C 입니다.")
    }
    
    else if (Score < 70 && Score >=50)
    {
        printf("학점이 D 입니다.")
    }

    else{

        printf(" 학점이 F 입니다.");
    }

    return 0;

}*/